%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%%%%%%%%%%%%%%%%%%%  INGRESAR LAS PREGUNTAS DE LA  EVALUACIÓN %%%%%%%%%%%%%%%%%%%%%%%%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\paragraph{I} Algebra Review. Read the following questions and justify your answer with a procedure.

\begin{preguntas}

%\pregunta[13] Solve the following equation,
%\begin{gather*}
%    f(x)=e^{2x} - 3e^{x}=-2
%\end{gather*}
%\begin{center}
%    \framebox(\textwidth,150){}   
%\end{center}

\pregunta[12] Find the asymptotes and intersections of the following function, 
\begin{gather*}
    g(x) = \frac{4x^2-1}{2x^2-1}
\end{gather*}

\begin{center}
    \framebox(\textwidth,150){}   
\end{center}

%\newpage

\pregunta[12] Consider that $h(x)=e^{x}$ and $j(x)=1-x$. 
Find the following operation \( m(x) = \qty(h\circ j)(x). \)

\begin{center}
    \framebox(\textwidth,125){} 
\end{center}

%\(n(x)\)
%\begin{center}
%    \framebox(\textwidth,150){} 
%\end{center}

\newpage

\pregunta[13] Consider that $h(x)=e^{-x^2}$.
Find the following operation \(h^{-1}(x)\)
\begin{center}
    \framebox(\textwidth,100){} 
\end{center}

\end{preguntas}


\paragraph{II} Limits. Read the following questions and justify your answer with a procedure.


\begin{preguntas}

\pregunta[20] Identify the discontinuities of the function (vertical asymptotes or holes) and compute the limit using the numeric approach at those values of the function \(f(x)=\frac{x^2-4}{x-2}.\)
Compute the limit from the left and right.

\begin{center}
    \framebox(\textwidth,160){} 
\end{center}


%\newpage

%Create a table with at least 3

%\pregunta[5] Identify the discontinuities of the function (vertical asymptotes or holes) and compute the limit using the numeric approach at those values of the function \(f(x)=\frac{x^2-4}{x-2}.\)


\pregunta[20] Compute the limit using the numeric approach at those values of the function \(f(x)=\frac{\sin(x)}{x}.\)
Compute the limit from the left and right.
%Use the page 4 to write your argument.

\begin{center}
    \framebox(\textwidth,160){} 
\end{center}


\newpage

\begin{minipage}{0.6\textwidth}
\pregunta[10] Applying the graphical technique to compute the limit when $x\to 0$.
Argument if the limit converges.
Use the notion of limit from the left and right.
\end{minipage}
\begin{minipage}{0.4\textwidth}
\begin{tikzpicture}[
    scale=1.2,
    >=Stealth,
    declare function={
        f(\x) = \x/abs(\x); % Función |x|/x
    }
]
    % Definir dimensiones (ratio 5:4)
    \def\xmin{-2.5} \def\xmax{2.5}   % Rango en x
    \def\ymin{-2.5} \def\ymax{2.5} % Rango en y
    \def\tick{0.2} % Tamaño de las marcas

    % Grid mayor (cada 1 unidad)
    \draw[gray!200, very thin] (\xmin,\ymin) grid (\xmax,\ymax);
    % Grid menor (cada 0.5 unidades)
    \foreach \x in {-2.5,-2,...,2.5} {
        \draw[gray!100, very thin] (\x,\ymin) -- (\x,\ymax);
    }
    \foreach \y in {-2.5,-2,...,2.5} {
        \draw[gray!100, very thin] (\xmin,\y) -- (\xmax,\y);
    }

    % Ejes
    \draw[->, thick] (\xmin-0.5,0) -- (\xmax+0.5,0) node[right] {$x$};
    \draw[->, thick] (0,\ymin-0.5) -- (0,\ymax+0.5) node[above] {$y$};

    % Marcas en los ejes (cada 1 unidad)
    \foreach \x in {-2,-1,1,2} {
        \draw[thick] (\x,-\tick) -- (\x,\tick) node[below=2pt] {$\x$};
    }
    \foreach \y in {-2,-1,1,2} {
        \draw[thick] (-\tick,\y) -- (\tick,\y) node[left=2pt] {$\y$};
    }

    % Gráfica de la función
    \draw[very thick, blue, domain=\xmin:-0.01, samples=50] plot (\x, {f(\x)});
    \draw[very thick, blue, domain=0.01:\xmax, samples=50] plot (\x, {f(\x)});

    % Discontinuidad en x=0 (círculos huecos)
    \filldraw[draw=blue, fill=white, thick] (0,1) circle (2.5pt);
    \filldraw[draw=blue, fill=white, thick] (0,-1) circle (2.5pt);

    % Etiqueta de la función
    %\node[blue, anchor=south east] at (4.5,1.5) {$f(x)=\frac{|x|}{x}$};
\end{tikzpicture}

\end{minipage}


\newpage


\begin{minipage}[c]{0.6\textwidth}
\pregunta[13] Applying the graphical technique to compute the limit when $x\to 0$.
Argument if the limit converges.
Use the notion of limit from the left and right.
\end{minipage}
\hfill
\begin{minipage}[c]{0.4\textwidth}
\begin{tikzpicture}
% Definir los límites de los ejes para mantener ratio 5:4
\def\xmin{-2.5}
\def\xmax{2.5}
\def\ymin{-2.5}
\def\ymax{2.5}

% Dibujar la cuadrícula menor (más clara)
\draw[step=0.2, gray!100, very thin] (\xmin,\ymin) grid (\xmax,\ymax);

% Dibujar la cuadrícula mayor (más oscura)
\draw[step=1, gray!200, thin] (\xmin,\ymin) grid (\xmax,\ymax);

% Dibujar los ejes
\draw[->, thick] (\xmin,0) -- (\xmax,0) node[right] {$x$};
\draw[->, thick] (0,\ymin) -- (0,\ymax) node[above] {$y$};

% Marcar los ticks en los ejes
\foreach \x in {-2,-1,0,1,2}
  \draw (\x,0.05) -- (\x,-0.05) node[below] {\x};
\foreach \y in {-2,-1,0,1,2}
  \draw (0.05,\y) -- (-0.05,\y) node[left] {\y};

% TRAMO 1: 0.5*(x+1)x³/(x+1) = 0.5x³ para x ≠ -1, dominio: -2 < x < 0
% Dibujar desde -2+0.01 hasta -1-0.01
\draw[blue, thick, domain=-1.59:-1.01, smooth, variable=\x] plot ({\x}, {0.5*(\x)^3});
% Dibujar desde -1+0.01 hasta 0-0.01
\draw[blue, thick, domain=-0.99:0, smooth, variable=\x] plot ({\x}, {0.5*(\x)^3});
% Hueco en x=-1 (límite existe pero función no definida)
\draw[blue, thick, fill=white] (-1,{0.5*(-1)^3}) circle (3pt); % hueco en (-1, -0.5)

\draw[blue, thick, fill=blue] (0,0) circle (3pt); % hueco en (-1, -0.5)

% TRAMO 2: x² para 0 ≤ x < 1
\draw[red, thick, domain=0:0.99, smooth, variable=\x] plot ({\x}, {\x*\x+1});
% Punto relleno en x=0 (está incluido en el dominio)
\draw[red, thick, fill=red] (0,1) circle (2.5pt);
% Círculo hueco en x=1 (límite por la izquierda = 1, pero no está en este tramo)
\draw[red, thick, fill=white] (1,2) circle (2.5pt);


% TRAMO 3: √(x-1)+1 para 1 ≤ x
% Dibujar desde x=1 hasta x=2.5 (ajustado para no salir del rango)
\draw[green!70!black, thick, domain=1:2.5, smooth, variable=\x] plot ({\x}, {sqrt(\x-1)+1});
% Punto relleno en x=1 (está incluido en el dominio)
\draw[green!70!black, thick, fill=green!70!black] (1,1) circle (2.5pt);

% Líneas punteadas de referencia en puntos importantes
%\draw[dashed, gray!40, thin] (-1,\ymin) -- (-1,\ymax);
%\draw[dashed, gray!40, thin] (0,\ymin) -- (0,\ymax);
%\draw[dashed, gray!40, thin] (1,\ymin) -- (1,\ymax);

% Etiquetas de los puntos de discontinuidad
%\node[below, gray] at (-1,-2.1) {$x=-1$};
%\node[below, gray] at (0,-2.1) {$x=0$};
%\node[below, gray] at (1,-2.1) {$x=1$};

% Leyenda explicativa
%\node[draw=black, rounded corners=3pt, fill=white, inner sep=5pt, align=left, font=\footnotesize, anchor=north west] at (-2.4,1.8) {
%  $\bullet$ Círculo hueco: límite/hueco\\ 
%  $\bullet$ Círculo relleno: valor de función
%};

% Etiquetas de las funciones por tramos
%\node[blue, above] at (-1.5, -1.5) {$0.5\frac{(x+1)x^3}{x+1}$};
%\node[blue, below] at (-1.5, -1.5) {$-2 < x < 0$};
%\node[red, above] at (0.5,0.5) {$x^2$};
%\node[red, below] at (0.5,0.5) {$0 \le x < 1$};
%\node[green!70!black, above] at (1.8,2) {$\sqrt{x-1}+1$};
%\node[green!70!black, below] at (1.8,2) {$1 \le x$};

% Anotaciones de los huecos y límites
\node[blue, below] at (-1, -0.7);
\node[red, above right] at (1,1.1);
\node[red, above] at (0.1, -0.5);

% Origen
%\node[below left] at (0,0) {$O$};

\end{tikzpicture}
\end{minipage}




%\begin{figure}[h]
%\raggedright

%\caption{Gráfica de función seccionada con tres tramos. Se marcan los huecos y límites con notación de círculos huecos (límites/huecos) y círculos rellenos (valores de la función).}
%\label{fig:func_seccionada_3tramos}
%\end{figure}



\begin{comment}



\pregunta[5] Applying the graphical technique to compute the limit when $x\to 1$.
Argument if the limit converges.
Use the notion of limit from the left and right.

\begin{tikzpicture}[
    scale=1.2,
    >=Stealth,
    declare function={
        f(\x) = 3/((\x-1)^2); % Función 3/(x-1)^2
    }
]
    % Definir dimensiones (ratio 5:4)
    \def\xmin{-4} \def\xmax{6}      % Rango en x (10 unidades, ratio 5:4)
    \def\ymin{-1} \def\ymax{7}      % Rango en y (8 unidades, ratio 5:4)
    \def\tick{0.2} % Tamaño de las marcas
    \def\asintota{1} % Asíntota vertical en x=1

    % Grid mayor (cada 1 unidad)
    \draw[gray!30, very thin] (\xmin,\ymin) grid (\xmax,\ymax);
    % Grid menor (cada 0.5 unidades)
    \foreach \x in {-4,-3.5,...,6} {
        \draw[gray!10, very thin] (\x,\ymin) -- (\x,\ymax);
    }
    \foreach \y in {-1,-0.5,...,7} {
        \draw[gray!10, very thin] (\xmin,\y) -- (\xmax,\y);
    }

    % Ejes
    \draw[->, thick] (\xmin-0.5,0) -- (\xmax+0.5,0) node[right] {$x$};
    \draw[->, thick] (0,\ymin-0.5) -- (0,\ymax+0.5) node[above] {$y$};

    % Marcas en los ejes (cada 1 unidad)
    \foreach \x in {-4,-3,...,6} {
        \draw[thick] (\x,-\tick) -- (\x,\tick) node[below=2pt] {$\x$};
    }
    \foreach \y in {-1,0,1,2,3,4,5,6,7} {
        \draw[thick] (-\tick,\y) -- (\tick,\y) node[left=2pt] {$\y$};
    }

    % Gráfica de la función (evitando la asíntota)
%    \draw[very thick, blue, domain=\xmin:0.7, samples=50, smooth] 
%        plot (\x, {f(\x)});
%    \draw[very thick, blue, domain=1.3:\xmax, samples=50, smooth] 
%        plot (\x, {f(\x)});

%\draw[very thick, blue, domain=\xmin:-1, samples=50, smooth] plot (\x, {f(\x)});
%\draw[very thick, blue, domain=3:\xmax, samples=50, smooth] plot (\x, {f(\x)});


\draw[very thick, blue, domain=\xmin:0.35, samples=50, smooth] plot (\x, {f(\x)});
\draw[very thick, blue, domain=1.65:\xmax, samples=50, smooth] plot (\x, {f(\x)});

    % Puntos notables
    %\filldraw[blue] (0,3) circle (2pt) node[above left] {$(0,3)$};
    %\filldraw[blue] (2,3) circle (2pt) node[above right] {$(2,3)$};
    %\filldraw[blue] (4,0.333) circle (2pt) node[above right] {$(4,\frac{1}{3})$};
    %\filldraw[blue] (-2,0.333) circle (2pt) node[above left] {$(-2,\frac{1}{3})$};

    % Etiqueta de la función
    \node[blue, anchor=south west] at (-3,6) {$f(x)=\dfrac{3}{(x-1)^2}$};
    
    % Comportamiento asintótico
    %\node[anchor=north east, font=\small] at (6,7) {$\lim\limits_{x\to 1^{\pm}} f(x)=+\infty$};
    %\node[anchor=south east, font=\small] at (6,0) {$\lim\limits_{x\to\pm\infty} f(x)=0$};
    
    % Indicar discontinuidad con notación
    %\node[red] at (1,1.5) {$\times$};
    %\node[red, below] at (1,1.2) {Discontinuidad};
    %\node[red] at (1,2) {$x=1$};
\end{tikzpicture}
\end{comment}

\end{preguntas}

